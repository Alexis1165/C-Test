<UserControl x:Class="BookParser.View.BookGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,0,0,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView Name="BooksGrid" Grid.Row="1" ItemsSource="{Binding BooksList}"  >
            <ListView.View>
                <GridView x:Name="grdBook">
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}"  Width="50"/>
                    <GridViewColumn Header="Author" DisplayMemberBinding="{Binding Author}"  Width="80" />
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price}" Width="100" />
                    <GridViewColumn Header="Year" DisplayMemberBinding="{Binding Year}" Width="80" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
